## Pointers

[From ALX Concepts](https://alx-intranet.hbtn.io/concepts/60)

- Every time you declare a variable, the computer will reserve memory for this variable. The memory reserved will then store the value of the variable. A pointer in this case stores the address where the variable is stored/